--- a/webapp/src/app/api/auth/login/route.ts
+++ b/webapp/src/app/api/auth/login/route.ts
@@ -45,7 +45,7 @@
     response.cookies.set(AUTH_COOKIE_NAME, token, {
       httpOnly: true,
       sameSite: 'lax',
-      secure: false,
+      secure: process.env.NODE_ENV === 'production',
       path: '/',
       maxAge: 7 * 24 * 60 * 60, // 7 days
     })
